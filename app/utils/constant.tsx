export const ROUTES = {
    LOGIN : "/login",
    REGISTER : "/register",
    DASHBOARD : "/dashboard"
}

const BASE_URL = "http://localhost:3000"

export const API_ROUTES = {
    GET_ALL_USERS : `${BASE_URL}/api/users/get-all-users`
}